# ── Full project requirements (installs everything) ────────────────────────────
# Run from: project root
# Install:  pip install -r requirements.txt

# Frontend (Reflex)
reflex>=0.6.0

# Backend (FastAPI + ML)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
pandas>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.0.0
tqdm>=4.66.0