# ── Frontend requirements ──────────────────────────────────────────────────────
# Run from: project root (where rxconfig.py lives)
# Install:  pip install -r requirements-frontend.txt
# Start:    reflex run

reflex>=0.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
